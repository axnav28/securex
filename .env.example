# Frontend API origin used by the browser
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Comma-separated browser origins accepted by the API
CORS_ORIGINS=http://localhost:3000

# Demo-only JWT signing secret; use a long random value outside local demos
SECUREX_JWT_SECRET=replace-with-a-long-random-local-secret
