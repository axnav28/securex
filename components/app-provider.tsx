'use client';
import {createContext,useContext,useEffect,useState} from 'react';import {apiFetch} from '@/lib/api';
type AppState={dark:boolean;setDark:(v:boolean)=>void;org:string;setOrg:(v:string)=>void;token:string|null;signIn:(email:string,password:string)=>Promise<void>;signOut:()=>void};
export const AppContext=createContext<AppState>({dark:false,setDark:()=>{},org:'Asteria Finance',setOrg:()=>{},token:null,signIn:async()=>{},signOut:()=>{}});
export function AppProvider({children}:{children:React.ReactNode}){const [dark,setDark]=useState(false);const [org,setOrg]=useState('Asteria Finance');const [token,setToken]=useState<string|null>(null);useEffect(()=>{setDark(localStorage.getItem('securex-theme')==='dark');setOrg(localStorage.getItem('securex-org')||'Asteria Finance');setToken(localStorage.getItem('securex-token'))},[]);useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light';localStorage.setItem('securex-theme',dark?'dark':'light')},[dark]);useEffect(()=>{localStorage.setItem('securex-org',org)},[org]);const signIn=async(email:string,password:string)=>{const data=await apiFetch<{access_token:string}>('/api/v1/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('securex-token',data.access_token);setToken(data.access_token)};const signOut=()=>{localStorage.removeItem('securex-token');setToken(null)};return <AppContext.Provider value={{dark,setDark,org,setOrg,token,signIn,signOut}}>{children}</AppContext.Provider>}
export const useApp=()=>useContext(AppContext);export const useTheme=useApp;
